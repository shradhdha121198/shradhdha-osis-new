<section class="idx-story-section" data-section="our_story">
  <div class="row">
    {% if section.settings.image != blank %}
    <div class="storey-image half">
      <img  loading="lazy" src="{{ section.settings.image | img_url: "master" }}" alt="Story">
    </div>
    {% endif %}
    <div class="storey-content half">
      <div class="idx-story-content">
        {% if section.settings.text != blank %}
        <h2 class="h3">{{ section.settings.text }}</h2>
        {% endif %}
        {% if section.settings.richtext != blank %}
        <div class="desc">
          {{ section.settings.richtext }}
        </div>
        {% endif %}
        {% if section.settings.btn_text %}
        <div class="button-wrap button6">
          <a href="{% if section.settings.btn_url %}{{ section.settings.btn_url }}{% else %}#{% endif %}">{{ section.settings.btn_text }}</a>
        </div>
        {% endif %}
      </div>
      <div class="accordian_box_main">
        <div class="accordian_box">
          <div class="acc_par_main">
            <div class="acc_button" style="margin-top: 10px;">Accordian Button</div>
            <p class="acc_par">Let's make a nice big leafy tree. You have to make those little noises or it won't work. Let's put some happy little clouds in our world.</p>
          </div>
          <div class="acc_par_main">
            <div class="acc_button">Accordian Button</div>
            <p class="acc_par">Let's make a nice big leafy tree. You have to make those little noises or it won't work. Let's put some happy little clouds in our world.</p>
          </div>
          <div class="acc_par_main">
            <div class="acc_button">Accordian Button</div>
            <p class="acc_par">Let's make a nice big leafy tree. You have to make those little noises or it won't work. Let's put some happy little clouds in our world.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
<style>
  .button6 a{
    position: relative;
    width: auto;
    font-weight: "DIN-Bold";
    border-radius: 0;
    text-align: center;
    border: 1px solid #7CA7AD;
    text-decoration: none;
    display: inline-block;
    cursor: pointer;
    font-size: 20px;
    line-height: 1.3;
    letter-spacing: 1;
    padding: 10px 20px;
    text-transform: uppercase;
    min-width: 208px;
    letter-spacing: 1px;
    background: transparent;
    color: #fff;
  }
  .button6 a:hover{
    background: transparent !important;
    color: #7ca7ad !important;
  }
  .button6 a:before{
    position: absolute;
    content: "";
    display: block;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    transform: scale(1, 1);
    transform-origin: left center;
    z-index: -1;
    background: #7CA7AD;
    color: #fff;
    transition: transform 0.45s cubic-bezier(0.785, 0.135, 0.15, 0.86);
  }
  .button6 a:hover:before {
    transform-origin: right center;
    transform: scale(0, 1);
  }
  .acc_par{
    display: none;
  }
  .acc_button{
    position: relative;
    width: auto;
    font-weight: "DIN-Bold";
    border-radius: 0;
    text-align: center;
    border: 1px solid #7CA7AD;
    text-decoration: none;
    display: inline-block;
    cursor: pointer;
    font-size: 20px;
    line-height: 1.3;
    letter-spacing: 1;
    padding: 10px 20px;
    text-transform: uppercase;
    min-width: 208px;
    letter-spacing: 1px;
    background: #7CA7AD;
    color: #fff;
    margin-bottom: 10px;
    margin-left: 20px;
  }
  .acc_button:hover{
    background: #000;
    color: #fff;
    border-color: #000;
  }
  .acc_par_main p{
    margin-bottom: 10px;
    margin-left: 20px;
  }
</style>
<script>
  $('.accordian_box_main .accordian_box .acc_par_main').on("click", function(){
    if($(this).hasClass('open')){
      $(this).removeClass('open');
      $(this).find('.acc_par').slideUp();
    }else{
      $('.acc_par').slideUp();
      $(this).addClass('open');
      $(this).find('.acc_par').slideDown();
    }
  });
</script>
{% schema %}
  {
    "name": "Our Story",
    "settings": [
		{
			"type": "image_picker",
			"id": "image",
			"label": "Select Image"
		},
		{
			"type": "text",
			"id": "text",
			"label": "Title"
		},
		{
			"type": "richtext",
			"id": "richtext",
			"label": "Select Text"
		},
		{
			"type": "text",
			"id": "btn_text",
			"label": "Button Title"
		},
		{
			"type": "url",
			"id": "btn_url",
			"label": "Button Url"
		}
	],
	"presets": [
		{
			"name": "Our Story",
			"category": "Our Story"
		}
	]
  }
{% endschema %}